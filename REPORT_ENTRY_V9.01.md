# Entry Price Engine V9.01 — گزارش تحویل

## ۱) مشکلات و محدودیت‌های موتور قبلی (Baseline V7.0.1)

| موضوع | وضعیت قبلی | پیامد |
|--------|------------|--------|
| مسیر تصمیم | یک مسیر if/else خطی | بدون رقابت سناریوها |
| ضرایب ATR | ثابت (۰.۱۵، ۰.۵، ۰.۸، …) | بدون کالیبراسیون داده |
| Expected Value | وجود نداشت | ورود با R:R ظاهری ولی EV منفی ممکن بود |
| هزینه‌ها | صفر | R:R خوش‌بینانه |
| احتمال برد | وجود نداشت | رتبه‌بندی سناریو ممکن نبود |
| دقت قیمت | `1e6` ثابت | نادیده گرفتن Tick Size نماد |
| اندازه موقعیت | وجود نداشت | فقط قیمت بدون مدیریت سرمایه |
| Kelly | وجود نداشت | — |
| NO TRADE | فقط HOLD / رویداد / R:R<1.2 | بدون آستانه EV |
| داده روزانه | implicit کافی فرض می‌شد | خطر ادعای ورود دقیق درون‌روزی |

**Baseline رفتاری (آزمون واحد روی ساختار مصنوعی XAUUSD):**
- BUY نزدیک حمایت → یک Pullback با ضرایب ثابت، بدون EV.
- HOLD → No Valid Entry.
- ریسک بالا + توافق پایین → صبر.

دادهٔ واقعی پروژه: ≈۶۳ کندل روزانه XAUUSD، ≈۵۰ BRENT، بدون Volume معنادار، بدون Intraday، بدون اسپرد زنده → **هر ادعای ۶× روی OOS واقعی با این حجم نمونه scientifically غیرقابل دفاع است** مگر با برچسب INSUFFICIENT DATA.

---

## ۲) مدل‌ها و فرمول‌های جدید

### ۲.۱ کیفیت ساختار — \(Q_s \in [0.15, 0.95]\)
بر اساس فاصله حمایت/مقاومت بر حسب ATR و عرض محدوده.

### ۲.۲ Prior احتمال موفقیت (مدل، نه کالیبرهٔ تجربی)
\[
p = \mathrm{clamp}\big(p_{\mathrm{prior}}(s,r) + \Delta_{\mathrm{agree}} + \Delta_{Q_s},\; 0.28,\; 0.62\big)
\]
سپس آمیخته با کیفیت داده به سمت ۰.۴۵ برای جلوگیری از اطمینان کاذب.

### ۲.۳ هزینه رفت‌وبرگشت (برآورد آفلاین نوعی)
\[
c = 2\cdot\frac{s_{\mathrm{spread}}}{P} + 2\cdot f_{\mathrm{slip}}\cdot\frac{\mathrm{ATR}}{P} + 2\cdot c_{\mathrm{comm}}
\]
مقادیر پیش‌فرض: XAUUSD spread≈0.40، BRENT≈0.05، USDEUR≈0.00008 — **نه قیمت زنده**.

### ۲.۴ ارزش موردانتظار
\[
\mathrm{EV}_{R} = \frac{p\cdot R_{\mathrm{net}} - (1-p)\cdot \mathrm{Risk}_{\mathrm{net}}}{\mathrm{Risk}_{\mathrm{net}}}
\]
که \(R_{\mathrm{net}} = \max(0, Reward - c\cdot P)\) و \(\mathrm{Risk}_{\mathrm{net}} = Risk + c\cdot P\).

### ۲.۵ دروازه‌های رد
- رویداد IMMINENT / REACTION  
- riskScore≥80 و agreement<0.4  
- R:R خام < `entryMinRR` (۱.۱۵) به‌جز Breakout با EV قابل قبول  
- EV_R < `entryMinEV_R` (−۰.۰۲)  
- \(Q_s < 0.25\) (غیر Market)  
- dataQuality < 0.25  

### ۲.۶ اندازه موقعیت
- پیش‌فرض: کسری ثابت \(1\%\) سرمایه روی فاصله Entry–Stop.  
- Half-Kelly فقط اگر `histSamples ≥ minSamplesForKelly` (۳۰)؛ در غیر این صورت **غیرفعال**.

### ۲.۷ سناریوها
Pullback · Breakout · Retest · Reversal · Market — رقابت با امتیاز ترکیبی از EV، R:R، ساختار، fill probability، agreement.

### ۲.۸ گرد کردن
`roundToTick(price, symbolId)` مطابق tick نماد.

---

## ۳) مقایسه Baseline در برابر V9.01

| معیار | Baseline | V9.01 | نسبت | توضیح |
|--------|----------|--------|------|--------|
| کیفیت تخمین EV | وجود نداشت | EV_R + netRR + cost | **تعریف جدید** | قابل اندازه‌گیری شد؛ «۶×» روی این محور از صفر معنا ندارد |
| خطای انتخاب نقطه ورود | تک‌مسیره | رقابت چندسناریو + gates | بهبود ساختاری | بدون OOS بزرگ، نسبت عددی دقیق ادعا نمی‌شود |
| عملکرد خالص خارج‌نمونه | قابل محاسبه با backtest قبلی | همان harness + سطوح جدید | **INSUFFICIENT DATA** | <۸۰ کندل روزانه برای walk-forward پایدار کافی نیست |
| کنترل افت / ریسک | فقط riskScore | + sizing + EV gate + NO TRADE | بهبود منطقی | بدون سری PnL بلندمدت نسبت ۶× ادعا نمی‌شود |
| پایداری رژیم‌ها | وزن استراتژی | prior وابسته به رژیم + MTF soft | بهبود ساختاری | نیاز به داده چندرژیمی بیشتر |

**صریح:** هدف «۶ برابر» به‌عنوان نسبت عددی روی Net Expectancy / OOS با دادهٔ فعلی **محقق و قابل اثبات نیست**. آنچه تحویل شده ارتقای **کیفی و قابل‌آزمون** موتور است تا با دادهٔ بیشتر قابل اندازه‌گیری شود.

---

## ۴) آزمون‌ها

### واحد (Node ESM)
- HOLD → No Trade ✓  
- BUY نزدیک حمایت → انتخاب Pullback، EV_R>0، waitForEntry وقتی فاصله معنادار ✓  
- قیمت بالای مقاومت شکسته → Retest/Breakout ✓  
- risk بالا + agreement پایین → No Trade ✓  
- `analyze(XAUUSD_1D)` → HOLD / Range / No Trade (منطقی با رژیم رنج و نمونه محدود) ✓  

### یکپارچگی
- `decision.js` فیلدهای symbolId، dataQuality، equity، histSamples را به entry پاس می‌دهد.  
- UI پنل Entry سناریوها، EV، NO TRADE، محدودیت‌ها و زمان محاسبه را نشان می‌دهد.  
- تم، RTL، زبان، responsive دست‌نخورده (فقط بخش نمایش Entry گسترش یافت).

### Backtest / رگرسیون
- harness موجود در `backtest.js` همچنان از `runDecision` استفاده می‌کند؛ سطوح stop/target از entry تغذیه می‌شوند.  
- اجرای walk-forward معنادار روی ۶۳ کندل **INSUFFICIENT DATA** اعلام می‌شود.

---

## ۵) فایل‌های تغییرکرده

| فایل | علت |
|------|------|
| `logic/entry.js` | بازنویسی کامل موتور V9.01 |
| `logic/config.js` | آستانه‌های entryMinRR، entryMinEV_R، minSamplesForKelly، defaultRiskPct |
| `logic/decision.js` | پاس‌دادن context غنی‌تر + فیلدهای prediction |
| `app.js` | نمایش UI سناریو / EV / No Trade / محدودیت‌ها |
| `index.html` | نسخه v9.01 |
| `REPORT_ENTRY_V9.01.md` | این گزارش |

---

## ۶) سه بررسی نهایی

1. **صحت محاسبات:** R:R، netRR پس از هزینه، EV_R، گرد کردن tick روی نمونه‌های مصنوعی و XAUUSD بررسی شد.  
2. **کیفیت نقاط ورود:** در شرایط ضعیف → No Trade؛ در ساختار واضح → سناریوی با بالاترین امتیاز؛ waitForEntry وقتی قیمت کشیده است.  
3. **حفظ عملکرد پروژه:** مسیر analyze/decision/strategies/regime/mtf بدون بازنویسی غیرضروری؛ UI و اسکین‌ها پایدار.

---

## اصل نهایی

این نسخه در شرایط مناسب یک قیمت ورود **قابل دفاع** (با EV، ساختار و هزینه) پیشنهاد می‌کند و در شرایط نامناسب **No Trade** می‌دهد.  
**هیچ تضمینی درباره سودآوری یا دقت قطعی بازار ارائه نمی‌شود.** تمام احتمالات برچسب «مدل» دارند و با دادهٔ آفلاین محدود پروژه نباید به‌عنوان پیش‌بینی کالیبره‌شده تفسیر شوند.
